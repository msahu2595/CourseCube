diff --git a/node_modules/react-native-animated-tree-view/ListView/Recursive.js b/node_modules/react-native-animated-tree-view/ListView/Recursive.js
index 976dd0e..9d3d496 100644
--- a/node_modules/react-native-animated-tree-view/ListView/Recursive.js
+++ b/node_modules/react-native-animated-tree-view/ListView/Recursive.js
@@ -28,7 +28,7 @@ export default Apps = props => {
   let cNN = childrenNodeName ? childrenNodeName : 'items';
 
   const selectAccountFunc = (selectedOptions, option) => {
-    if (option[cNN]) {
+    if (option[cNN] && option[cNN].length) {
       LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut);
       setSelectedOptions({ ...selectedOptions });
     } else {
@@ -150,7 +150,7 @@ const List = ({
         listItemStyle={{ ...listItemStyle }}
         textStyle={textStyle}
         rightImage={
-          items &&
+          !!items?.length &&
           (rightImage || {
             uri: 'http://www.pngmart.com/files/3/Down-Arrow-PNG-HD.png',
           })
